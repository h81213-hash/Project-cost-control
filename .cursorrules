# Antigravity 通用工程規則（Rules）

## 一、語言與輸出約定

* 所有回覆、說明、註釋、文件 **必須使用繁體中文**
* 代碼中的識別碼保持英文，不使用拼音
* 錯誤訊息、日誌內容允許為英文

## 二、技術默認約定

### 1. 後端架構 (FastAPI)
* **版本**: Python 3.12+ / FastAPI
* **數據處理**: 使用 `Pandas` 配合 `openpyxl` 引擎處理 Excel，確保能正確處理合併儲存格。
* **通訊**: 接口統一回傳 JSON 格式。
* **埠號**: 預設運行於 `http://localhost:8002`。

### 2. 前端架構 (Next.js)
* **框架**: Next.js 14+ (App Router)
* **語言**: TypeScript
* **樣式**: Tailwind CSS
* **通訊**: 使用 `fetch` 或 `axios` 連線至後端 `http://localhost:8002`。
* **埠號**: 預設運行於 `http://localhost:3002`。

### 3. API 設計規範
* **規律**: 遵循 RESTful API 設計原則。
* **CORS**: 必須明確允許 `http://localhost:3002` 的跨來源請求。

## 三、通用代碼規範

### 命名規範
* 變數 / 函數：`camelCase`
* 類別 / 組件：`PascalCase`
* 常數：`UPPER_SNAKE_CASE`
* 檔案 / 資料夾：`kebab-case`

### 註釋規範（強制）
* 註釋用於解釋「為什麼這樣設計」，而不是程式碼字面含義。
* 複雜邏輯、業務判斷、邊界條件必須寫註釋。
* 禁止無意義註釋。

**統一註釋標記：**
```ts
// TODO: 待實作功能
// FIXME: 已知問題或潛在缺陷
// NOTE: 重要設計說明
// HACK: 臨時方案，後續必須重構
```
